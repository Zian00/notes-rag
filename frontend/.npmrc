# eslint-plugin-jsx-a11y peer-dep declares eslint ^9 but we use ESLint v10
# (latest flat config). --legacy-peer-deps lets npm install without failing on
# this version mismatch; ESLint v10 is backwards-compatible with v9 plugins.
legacy-peer-deps=true
