import { HttpHandler } from "msw"

// MSW v2 request handlers — empty for Milestone A.
// Add route-specific handlers in later milestones (e.g., mock /api/documents, /api/chat).
const handlers: HttpHandler[] = []

export { handlers }
