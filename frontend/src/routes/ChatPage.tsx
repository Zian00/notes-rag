// Placeholder body for /chat and /chat/:conversationId — real chat UI lands
// in Milestone C. Keeps AppRoutes wireable end-to-end ahead of that work.
export function ChatPage() {
  return (
    <div className="p-6">
      <h1 className="font-heading text-xl font-semibold">Chat</h1>
      <p className="mt-2 text-sm text-muted-foreground">Chat coming in Milestone C.</p>
    </div>
  )
}
