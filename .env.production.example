# Copy to .env on the production VM and fill in real values.
# This file is checked into git as a template — never commit the real .env.

# Domain (used by Caddy for auto-HTTPS and CORS)
DOMAIN=notesrag.zheng00.me

# Postgres
POSTGRES_USER=notes
POSTGRES_PASSWORD=change-me-to-a-strong-password
POSTGRES_DB=notes_rag

# Auth
JWT_SECRET=change-me-to-a-long-random-string

# OpenAI
OPENAI_API_KEY=sk-your-openai-key

# LLM
LLM_MODEL=gpt-4o-mini
