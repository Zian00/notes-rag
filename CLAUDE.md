# notes-rag

Personal RAG study app — FastAPI + Postgres/pgvector + LangGraph backend,
React 19 + Vite + Tailwind v4 frontend.

## Agent skills

### Issue tracker

Issues live as GitHub issues in `Zian00/notes-rag`, managed with the `gh` CLI.
See `docs/agents/issue-tracker.md`.

### Triage labels

The five canonical triage roles, each label named after its role.
See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root.
See `docs/agents/domain.md`.
